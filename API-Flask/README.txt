Instalar flask desde el CMD:

1) pip install Flask

2) pip install Flask_restful

Generar el versionado para "requirements.txt":

3) pip freeze

4) pip freeze > requirements.txt (Aquì ingresa los datos en un archivo creado llamado "requirements.txt").

Iniciar un proyecto

1) visual studio con C#:

dotnet run -d AplicacionCliente.csproj

2) API FLASK : Flask -r miapi.py --run  